{
  "name": "claude-parallel-profiles",
  "displayName": "Claude Parallel Profiles",
  "description": "Companion for the Claude Code VS Code extension: run a different Claude account in each VSCode window simultaneously — capture, switch, and isolate accounts per window. Linux, WSL and macOS.",
  "version": "1.4.0",
  "publisher": "rivantmedia",
  "icon": "images/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/rivantmedia/claude-parallel-profiles"
  },
  "homepage": "https://github.com/rivantmedia/claude-parallel-profiles#readme",
  "bugs": {
    "url": "https://github.com/rivantmedia/claude-parallel-profiles/issues"
  },
  "license": "MIT",
  "author": {
    "name": "Rivant Media"
  },
  "contributors": [
    "dercasdrol (original author)"
  ],
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "claude",
    "claude code",
    "anthropic",
    "profiles",
    "profile manager",
    "accounts",
    "multi-account",
    "macos"
  ],
  "activationEvents": [
    "*"
  ],
  "main": "./dist/extension.js",
  "extensionKind": [
    "workspace"
  ],
  "capabilities": {
    "virtualWorkspaces": {
      "supported": false,
      "description": "Claude accounts live in data directories on the local file system, which virtual workspaces don't have."
    },
    "untrustedWorkspaces": {
      "supported": "limited",
      "description": "Works in Restricted Mode. The extension never reads or executes workspace content — it only uses the workspace folder path to tell windows apart."
    }
  },
  "contributes": {
    "commands": [
      {
        "command": "claudeProfiles.switchAccount",
        "title": "Switch Account for This Window",
        "category": "Claude Accounts"
      },
      {
        "command": "claudeProfiles.captureAccount",
        "title": "Save Current Account",
        "category": "Claude Accounts"
      },
      {
        "command": "claudeProfiles.showStatus",
        "title": "Account Quick Action",
        "category": "Claude Accounts"
      },
      {
        "command": "claudeProfiles.removeProfile",
        "title": "Forget a Saved Account",
        "category": "Claude Accounts"
      },
      {
        "command": "claudeProfiles.showLog",
        "title": "Show Log",
        "category": "Claude Accounts"
      }
    ]
  },
  "scripts": {
    "build": "node esbuild.js",
    "watch": "node esbuild.js --watch",
    "check-types": "tsc --noEmit",
    "compile": "npm run check-types && node esbuild.js",
    "vscode:prepublish": "npm run check-types && node esbuild.js --production",
    "vscode:uninstall": "node ./uninstall.js",
    "package": "vsce package --allow-star-activation",
    "release": "vsce publish --allow-star-activation"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
